[project]
name = "mcp-web-llm"
version = "0.1.0"
description = "Turn web versions of ChatGPT, Claude, and Gemini into MCP tools for your AI IDE."
dependencies = ["README.md"
requires-python = "==3.12.*"
dependencies = [
    "mcp>=1.25.0",
    "playwright>=1.58.0",
]
