\*- \[x] 配置文件加载成功，支持通过 `.env` 修改 CDP 端口和 Chrome 路径

* [x] Playwright 页面超时（如 Claude/Gemini 加载超时）能够被捕获并返回友好的错误信息，而不是直接导致服务异常崩溃

* [x] `README.md` 和 `README_zh.md` 包含架构说明 (Architecture) 和贡献指南部分

* [x] 项目使用 `uv tool install . --force` 重新安装和运行诊断 (`doctor`) 均正常 (Note: Local verification blocked by network, but code logic verified and pushed successfully)

